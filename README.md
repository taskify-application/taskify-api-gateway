# taskify-api-gateway
Centralized API Gateway built with Spring Cloud Gateway. Serves as the unified public entry point for the Flutter client, handling cross-origin resource sharing (CORS), request routing, rate limiting, and global security token validation.
